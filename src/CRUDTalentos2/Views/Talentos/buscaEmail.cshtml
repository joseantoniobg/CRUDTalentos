@model CRUDTalentos2.Models.Candidatos

<div class="row">
    <div class="col-lg-12"><h2>Por favor, escolha alguma das opções:</h2></div>
</div>
<div class="row">
    <div class="col-lg-6">
        <div class="panel panel-warning">
            <div class="panel-heading">Se você ainda não preencheu o formulário</div>
            <div class="panel-body">
                Caso você esteja submetendo o formulário pela primeira vez, por favor, clique no botão abaixo:<br /><br />
                <a asp-controller="Talentos" class="btn btn-success" asp-action="Index">Desejo me inscrever!</a>
            </div>
        </div>
    </div>
    <div class="col-lg-6">
        <div class="panel panel-success">
            <div class="panel-heading">Se você deseja atualizar os dados do seu formulário</div>
            <div class="panel-body">
                <p>Digite seu e-mail para que possamos buscar seus dados:</p>
                <form name="cand" asp-controller="Talentos" asp-action="buscaEmail">
                    <label for="email">E-mail:</label><br />
                    <input type="email" ng-model="Email" class="form-control" asp-for="@Model.Email" /><br />
                    <p ng-show="cand.Email.$invalid && !cand.Email.$pristine" class="help-block ng-hide">Informe um e-mail válido!</p>
                    @if (Model.Email.Equals("Não encontrado!"))
                    {
                        <p class="help-block">E-mail não encontrado!</p>
                        Model.Email = null;
                    }
                    <input type="submit" ng-disabled="cand.$invalid || cand.Email.$pristine" class="btn btn-info" value="Buscar meu formulário" />
                </form>
            </div>
        </div>
    </div>
</div>